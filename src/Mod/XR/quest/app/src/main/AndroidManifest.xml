<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: LGPL-2.1-or-later -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Networking: the LAN sync client (§3) and Google Drive. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- voice input (VoiceInput.java): transcripts are forwarded to the desktop -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- UDP broadcast discovery on 47811 needs the Wi-Fi state on some builds. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Optional: hand tracking, declared so the runtime offers the toggle. -->
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />

    <!-- Scoped storage: files come in through the Storage Access Framework,
         so no READ_EXTERNAL_STORAGE is needed on API 29+. -->

    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="true"
        android:version="1" />
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />
    <uses-feature
        android:name="com.oculus.feature.PASSTHROUGH"
        android:required="false" />

    <!-- Quest 2, Quest Pro and Quest 3 all run this build. -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:hasCode="true"
        android:extractNativeLibs="false"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest2|questpro|quest3" />
        <meta-data
            android:name="com.oculus.handtracking.frequency"
            android:value="HIGH" />
        <meta-data
            android:name="com.oculus.handtracking.version"
            android:value="V2.0" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:excludeFromRecents="false"
            android:resizeableActivity="false"
            android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode|density">

            <!-- Launching straight into VR: the Oculus category is what puts
                 the app in the headset's library rather than the flat panel
                 launcher. The LAUNCHER category is kept so `adb shell am
                 start` and the developer menu still work. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.oculus.intent.category.VR" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- A trampoline for the Storage Access Framework picker: the VR
             activity cannot show it while it holds the OpenXR session. -->
        <activity
            android:name=".FilePickerActivity"
            android:exported="false"
            android:theme="@style/TransparentTheme" />
    </application>
</manifest>
