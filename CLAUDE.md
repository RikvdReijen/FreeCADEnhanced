@AGENTS.md

# Claude Code notes

The line above imports **AGENTS.md**, the single source of truth — follow it. Claude Code does not read
`AGENTS.md` natively, so this `@AGENTS.md` import is how it loads here.
